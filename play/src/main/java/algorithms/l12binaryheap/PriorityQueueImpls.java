package algorithms.l12binaryheap;

/**
 * we can easily implement PQ with
 *
 * - sorted array / linked list
 *   - insert O(N)
 *   - peek O(1)
 *   - delete O(1)
 *
 * - unsorted array
 *   - insert O(1)
 *   - peek O(N)
 *   - delete O(N)
 *
 * - but we want look at the {@linkplain BinaryHeap}:
 *   - insert O(logN)
 *   - peek O(1)
 *   - delete O(logN)
 */
public class PriorityQueueImpls {
}
